---
- name: Deploy Next.js application 
  hosts: debian_servers
  become: yes
  tasks:
    - name: Pull Docker image
      docker_image:
        name: Villain1039532/netjs
        source: pull

    - name: Run Docker container
      docker_container:
        name: netjs
        image: Villain1039532/netjs
        state: started
        ports:
          - "3000:3000"
        volumes:
          - "/data"
  #   - name: Configure Nginx
  #     template:
  #       src: nginx.conf.j2
  #       dest: /etc/nginx/sites-available/nextjs_app
  #     notify:
  #       - Reload Nginx

  #   - name: Enable Nginx site
  #     file:
  #       src: /etc/nginx/sites-available/nextjs_app
  #       dest: /etc/nginx/sites-enabled/nextjs_app
  #       state: link
  #     notify:
  #       - Reload Nginx

  # handlers:
  #   - name: Reload Nginx
  #     service:
  #       name: nginx
  #       state: reloaded
